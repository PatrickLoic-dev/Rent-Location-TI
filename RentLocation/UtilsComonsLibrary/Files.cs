using System;
using System.Collections.Generic;
using System.Text;

namespace UtilsComonsLibrary
{
    public static class Files
    {
        //public static async WriteInTextFile(string FilePath, string TextLine)
        //{
         //   using (StreamWriter file = new(FilePath, append: true));
          //  await file.WriteLineAsync(TextLine);
        //}
    }
}
